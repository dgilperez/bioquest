import { HowXPWorksClient } from './page.client';

export const metadata = {
  title: 'How XP Works | BioQuest',
  description: 'Learn how Experience Points (XP) work in BioQuest and how to maximize your point earnings.',
};

export default function HowXPWorksPage() {
  return <HowXPWorksClient />;
}
